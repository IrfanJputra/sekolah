function succ(e) {
    let r = $(".sidebar.error");
    r.html() ? (r.children("h2").text("Success Report"), r.append(`<div class="alert alert-default">${e}</div>`)) : $("aside").append('<div class="sidebar error"><h2>Success Report</h2><div class="alert alert-default">' + e + "</div></div>"), setTimeout(() => {
        $("aside>.error").fadeOut(time), setTimeout(() => {
            $("aside>.error").remove()
        }, time)
    }, 3e4)
}
var v = ["label", "penulis", "populer", "recent", "random"];
v.forEach(function(e) {
    var r = `#${e}_id`;
    $.ajax({
        type: "POST",
        url: BASE_URL + "sidebar_get/" + e,
        cache: !1,
        dataType: "json",
        success: e => {
            "suc" == e.type && ($(r).html(e.view), setTimeout(() => {
                yall({
                    lazyClass: "malas"
                })
            }, 200), e.agenda && ($("#rand_agen").text("Agenda Akan Datang"), $(".wraptiga").addClass("agenda")))
        },
        error: (e, r) => {
            error_b(e.status, e.statusText, r)
        }
    })
});
var time = 1e3;